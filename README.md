# CodeAssistant



cd /src
uvicorn main:app --reload --host 0.0.0.0 --port 8000

pip install -r requirements.txt


sudo apt update
sudo apt install unrar


uvicorn main:app --reload --port 8000

# Terminal 2
cd codemind/server && cp .env.example .env && npm install && npm run dev

# Terminal 3
cd codemind/client && npm install && npm run dev